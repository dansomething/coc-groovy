/**
 * Available Groovy extension settings.
 */
export namespace Settings {
  /**
   * Name fo the Java home key in the config.
   */
  export const JAVA_HOME = 'java.home'

  /**
   * Name for the language server home key in the config.
   */
  export const LS_HOME = 'ls.home'

  /**
   * Name for the language server jvm args key in the config.
   */
  export const LS_VMARGS = 'ls.vmargs'

  /**
   * Name for the project referenced libraries key in the config.
   */
  export const REFERENCED_LIBRARIES = 'project.referencedLibraries'
}
